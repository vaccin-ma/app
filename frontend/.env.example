# Backend API URL — set this when the frontend is deployed (e.g. Vercel) so it calls your deployed backend.
# For local dev, leave unset to use http://localhost:8000
VITE_API_URL=https://your-backend-url.up.railway.app
